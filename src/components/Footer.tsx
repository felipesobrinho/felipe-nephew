import content from "@/data/content";

const Footer = () => (
  <footer className="py-8 px-6 text-center">
    <p className="font-mono text-xs text-muted-foreground">{content.footer}</p>
  </footer>
);

export default Footer;
